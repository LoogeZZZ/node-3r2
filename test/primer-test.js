const assert = require('assert')


it('Тестовый тест: адын равно адын', done => {
    assert.equal(1,1)
    done()
})

it('Тестовый тест: адын равно адын', done => {
    assert.equal(1,2)
    done()
})